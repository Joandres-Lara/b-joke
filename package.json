{
  "name": "b-joke",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "postinstall": "npm update -g npm && npm install pm2@latest -g && npm run build",
    "build": "rm -rf dist && npx babel src --out-dir dist",
    "start:dev": "npx babel-node src/index.js",
    "start": "npm run start:pm2",
    "start:pm2": "pm2-runtime start ecosystem.config.js --env production",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/cli": "^7.14.8",
    "@babel/core": "^7.14.8",
    "@babel/preset-env": "^7.14.8",
    "eris": "^0.15.1",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@babel/node": "^7.14.7",
    "jest": "^27.0.6",
    "jest-cli": "^27.0.6"
  }
}
